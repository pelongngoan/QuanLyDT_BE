export enum ROLE {
  ADMIN = "admin",
  TEACHER = "teacher",
  STUDENT = "student",
}

export enum STATE {
  ACTIVE = "active",
  LOCKED = "locked",
  PENDING = "pending",
}
export enum CLASSSTATE {
  OPEN = "active",
  CLOSE = "locked",
  INPROGRESS = "pending",
}
